# Composer dependencies (local dev tooling, e.g. phpcs)
vendor/
# A contrib module is a library, not an application — do not pin a lock file.
composer.lock

# Logs
*.log

# OS / editor
.DS_Store
.idea/
.vscode/

# Internal planning notes (owner policy: local-only, untracked)
.planning/
